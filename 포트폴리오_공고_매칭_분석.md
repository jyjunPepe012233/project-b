# Bundle Alchemist 프로젝트 - 채용 공고 매칭 분석 문서

> 대상 공고: 트릭컬 리바이브 Unity 클라이언트 개발자
> 분석 대상: bundle-alchemist (ProjectB) Unity 프로젝트
> 분석 일시: 2026-05-14

---

## 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [지원 자격별 사례 매칭](#지원-자격별-사례-매칭)
3. [우대 사항별 사례 매칭](#우대-사항별-사례-매칭)
4. [공고와 어울리지 않는 코드/구조 목록](#공고와-어울리지-않는-코드구조-목록)

---

## 프로젝트 개요

모바일 수집형 RPG를 목표로 한 Unity 2022.3 LTS 기반 프로젝트.
사도(Soldier) 수집, 소환, 레벨업, 장비 제작/장착, 상점, 소탕(Sweep), 사기(Morale) 충전 등의 핵심 콘텐츠 시스템을 갖추고 있으며, Hexagonal Architecture(Ports & Adapters) + VContainer DI + MVP UI 패턴으로 구성됨.

- **스크립트 수**: Assets/Scripts 하위 약 230개 이상의 .cs 파일
- **Assembly Definition**: 13개의 asmdef로 컴파일 단위 분리
- **씬 구성**: TitleScreen, Home, LoadingOverlay (3개 씬 + Additive 로딩)
- **외부 의존성**: VContainer, Spine, Firebase, URP, TextMeshPro

---

## 지원 자격별 사례 매칭

---

### 1. C# 언어에 대한 충분한 이해

> 면접관 기대: "C#의 런타임 특성을 이해하는가?" (GC, boxing, struct vs class, delegate/event)

#### 프로젝트 사례

**[사례 1-1] 제네릭 타입 제약(Generic Constraints)을 활용한 타입 안전성 확보**
- 파일: `Assets/Scripts/Core/Types/InterfaceRef.cs`
- `InterfaceRef<T> where T : class` — 제네릭 제약으로 값 타입 배제, 인터페이스 참조 전용 래퍼 구현
- lazy 캐싱 패턴 (`_value` 필드)으로 반복적인 `as` 캐스팅 비용 제거
- `IReadOnlyList<T>` 반환으로 외부 수정 방지 (불변성 보장)

**[사례 1-2] struct를 사용한 값 타입 데이터 설계**
- 파일: `Assets/Scripts/Data/Types/SoldierStatus.cs`, `SoldierStatusf.cs`
- 병사 스테이터스를 `struct`로 설계하여 힙 할당 없이 스택에서 처리
- 커스텀 연산자 오버로딩 (`operator *`) 구현으로 `SoldierStatus * SoldierStatusf` 곱셈 지원
- `[Serializable]` 적용으로 Inspector 직렬화도 동시 지원

```csharp
// SoldierStatus.cs:24-35
public static SoldierStatus operator *(SoldierStatus a, SoldierStatusf b)
{
    return new SoldierStatus
    {
        hp = (int)(a.hp * b.hp),
        physicalAttack = (int)(a.physicalAttack * b.physicalAttack),
        // ...
    };
}
```

**[사례 1-3] delegate/event 패턴의 일관된 사용**
- 파일: `Assets/Scripts/Infrastructure/UncaughtErrorCatcherService.cs:18`
- `public event Action<ErrorData> UncaughtErrorCaught` — 이벤트 기반 에러 전파
- `IDisposable` 구현으로 이벤트 구독 해제 보장 (`Application.logMessageReceived -= ...`)
- `AppDomain.CurrentDomain.UnhandledException` 후킹으로 네이티브 수준 예외까지 캐치

**[사례 1-4] FNV-1a 해시 알고리즘을 직접 구현한 중복 에러 필터링**
- 파일: `Assets/Scripts/Infrastructure/UncaughtErrorCatcherService.cs:85-108`
- `ulong hash = 14695981039346656037UL` (FNV offset basis) + `hash *= 1099511628211` (FNV prime)
- `Dictionary<ulong, DateTime>` 기반 시간 제한 중복 검사 (0.1초 이내 동일 에러 무시)
- 고성능 해시 비교로 문자열 비교 비용 회피

**[사례 1-5] LINQ 활용과 함수형 스타일 데이터 처리**
- 파일: `Assets/Scripts/Gameplay/SweepService.cs:108-109`
```csharp
_internalInventoryService.GiveItems(
    itemGains.Select(kvp => new ItemGain(kvp.Key, kvp.Value)), ItemGainAction.Reward);
```
- `Dictionary.Select()`로 데이터 변환 후 서비스에 전달하는 함수형 패턴

**[사례 1-6] IEnumerable/IEnumerator를 활용한 코루틴 기반 비동기 흐름**
- 파일: `Assets/Scripts/Infrastructure/SceneLoadingTask.cs:26-57`
- 로컬 함수(`IEnumerator Coroutine()`)로 코루틴 로직을 캡슐화
- `AsyncOperation`을 `yield return`으로 대기하는 Unity 비동기 패턴 적용

**[사례 1-7] Nullable 참조 처리와 방어적 프로그래밍**
- 파일: `Assets/Scripts/Gameplay/InternalInventoryService.cs:106-133`
- `FirstOrDefault()` 결과에 대한 null 체크 후 early return 패턴
- `TryConsumeItem` — Try 패턴(bool 반환)으로 실패 가능한 연산 표현

**[사례 1-8] IReadOnly 인터페이스 활용한 불변성 설계**
- `IReadOnlyPlayerData`, `IReadOnlyPlayerSoldier`, `IReadOnlyPlayerItem` — 읽기 전용 인터페이스
- `IPlayerData`가 `IReadOnlyPlayerData`를 확장하는 구조로, 수정 권한을 인터페이스 수준에서 제어
- 내부 컬렉션을 `IReadOnlyCollection<T>`, `IReadOnlyList<T>`로 노출하여 외부 수정 방지

**[사례 1-9] 이벤트 기반 데이터 변경 알림 (Observer 패턴)**
- `PlayerData`에서 속성 변경 시 `Action` 이벤트 발행:
  - `ExperienceChanged`, `LevelChanged`, `CoinsChanged`, `GemsChanged`, `MoraleChanged`, `FoodsChanged`
- UI Presenter가 이 이벤트를 구독하여 데이터 변경 시 자동 갱신 (예: `CoinLabelPresenter`)

**[사례 1-10] 열거형(enum)을 활용한 타입 안전 분류 체계**
- `CurrencyType`: Coins, Gems — 재화 종류 분류
- `ItemCategory`: Consumable, Equipment — 아이템 카테고리 분류
- `SoldierEquipmentSlot`: Slot1~Slot6 — 장비 슬롯 식별
- `SummonType`: Summon1x, Summon10x — 소환 유형 분류
- `ItemGainAction`: NoAction, Reward — 아이템 획득 후 행동 분류
- ShopService에서 `switch (shopItem.CurrencyType)` 패턴으로 재화별 로직 분기

---

### 2. 객체지향 프로그래밍(OOP)에 대한 충분한 이해

> 면접관 기대: "SOLID 적용 사례? 상속 vs 컴포지션 판단? 의존성 역전 경험?"

#### 프로젝트 사례

**[사례 2-1] Hexagonal Architecture (Ports & Adapters) 전면 적용**
- 프로젝트 전체가 포트/어댑터 패턴으로 구성됨
- 포트를 3계층으로 분류:
  - **Inbound Ports** (`Assets/Scripts/Gameplay/Ports/Inbound/`): UI/외부 → 비즈니스 로직 진입점 (16개 인터페이스)
  - **Outbound Ports** (`Assets/Scripts/Gameplay/Ports/Outbound/`): 비즈니스 로직 → 인프라 의존 (15개 인터페이스)
  - **Internal Ports** (`Assets/Scripts/Gameplay/Ports/Internal/`): 서비스 간 내부 의존 (5개 인터페이스)
- 이 구조로 인해 Gameplay 레이어는 Infrastructure, UI에 대한 직접 의존 없이 순수 비즈니스 로직만 포함

**[사례 2-2] 의존성 역전 원칙(DIP) — 인터페이스 기반 의존성 주입**
- 모든 서비스의 생성자가 인터페이스만 받음:
```csharp
// SoldierLevelUpService.cs:24-31
public SoldierLevelUpService(
    IPlayerSessionHolderPort playerSessionHolderPort,
    ISoldierStatusComputerPort soldierStatusComputerPort,
    ISoldierCombatPowerComputerPort soldierCombatPowerComputerPort)
```
- 구체 클래스가 아닌 Port 인터페이스에만 의존하여 테스트 시 Mock 교체 가능

**[사례 2-3] Template Method 패턴 — StructuredLifetimeScope**
- 파일: `Assets/Scripts/Dependency/Scopes/StructuredLifetimeScope.cs`
- `Configure()`가 sealed 역할을 하며, `AddInboundAdapters()`, `AddInternalAdapters()`, `AddOutboundAdapters()`, `AddData()` 순서로 호출
- 하위 클래스(HomeScreenLifetimeScope, ShopScreenLifetimeScope 등)가 필요한 메서드만 override
- 등록 순서가 강제되어 아키텍처 일관성 보장

**[사례 2-4] 인터페이스 분리 원칙(ISP) — 역할별 인터페이스 설계**
- `IReadOnlyPlayerData` vs `IPlayerData`: 읽기 전용 인터페이스와 수정 가능 인터페이스 분리
- `IReadOnlyPlayerSoldier` vs `IPlayerSoldier`: 동일한 분리 적용
- `IReadOnlyPlayerItem` vs `IPlayerItem`: 동일한 분리 적용
- UI 레이어는 ReadOnly 인터페이스만 접근, 수정은 Gameplay 서비스만 가능

**[사례 2-5] 단일 책임 원칙(SRP) — 서비스별 역할 분리**
- `SoldierStatusComputer`: 스탯 계산만 담당
- `SoldierCombatPowerComputer`: 전투력 계산만 담당
- `SoldierLevelUpService`: 레벨업 로직만 담당
- `SoldierEquipService`: 장비 장착만 담당
- `InternalInventoryService`: 아이템 수량 관리만 담당
- `InternalPlayerLevelUpService`: 플레이어 경험치/레벨 관리만 담당

**[사례 2-6] 개방-폐쇄 원칙(OCP) — LifetimeScope 확장**
- 새 화면 추가 시 `StructuredLifetimeScope`를 상속한 새 클래스만 작성하면 됨
- 기존 코드를 수정하지 않고 기능 확장 가능:
```csharp
// ShopScreenLifetimeScope.cs
public class ShopScreenLifetimeScope : StructuredLifetimeScope
{
    protected override void AddInboundAdapters()
    {
        base.AddInboundAdapters();
        RegisterPortAdapter<IShopServicePort, ShopService>();
    }
}
```

**[사례 2-7] Factory 패턴 — PlayerSoldierFactory**
- 파일: `Assets/Scripts/Gameplay/PlayerSoldierFactory.cs`
- `IPlayerSoldierFactory` 인터페이스를 통해 객체 생성 로직 추상화
- 초기 스탯 계산, 전투력 계산을 팩토리 내에서 일괄 처리 후 완성된 객체 반환

**[사례 2-8] 상속과 컴포지션의 적절한 사용 구분**
- **상속 사용**: `UIPresenter<TView> → UIComponent<TView>` (자율적 UI vs 외부 제어 UI)
- **상속 사용**: `SoldierCard → PlayerSoldierCard` (기본 카드에 플레이어 데이터 추가)
- **상속 사용**: `SoldierStatusItem → SoldierStatusUpgradeItem` (기본 스탯 표시에 비교 기능 추가)
- **컴포지션 사용**: 모든 서비스가 Port 인터페이스를 생성자 주입으로 보유 (has-a 관계)

**[사례 2-9] 리스코프 치환 원칙(LSP) — 제네릭 제약을 통한 컴파일 타임 보장**
```csharp
// StructuredLifetimeScope.cs
protected void RegisterPortAdapter<TPort, TAdapter>()
    where TPort : class
    where TAdapter : class, TPort  // TAdapter는 반드시 TPort를 구현해야 함
```

**[사례 2-10] 정적 데이터와 런타임 데이터의 계층적 분리**
- **Static Data** (인터페이스 + SO): `ISoldierData`, `IItemData`, `IChapterData` 등 — 기획 데이터, 불변
- **Runtime Data** (인터페이스 + 클래스): `IPlayerData`, `IPlayerSoldier`, `IPlayerItem` — 플레이어 진행 데이터, 가변
- Static ↔ Runtime 참조: `IPlayerSoldier.SoldierData`가 `ISoldierData`를 참조 (소유 관계)
- `Assets/Scripts/Data/` 디렉토리 자체가 `Static/`, `Runtime/`, `RuntimeImpl/`, `Types/`로 세분화

**[사례 2-11] SO 인터페이스 패턴 — Inspector 직렬화와 코드 추상화의 양립**
- ScriptableObject는 `[CreateAssetMenu]`로 Inspector에서 생성/편집 가능하게 하면서
- 인터페이스(`ISoldierData`, `IItemData` 등)로 추상화하여 코드에서는 SO 의존 없이 접근
- `InterfaceRef<T>` 커스텀 타입으로 Inspector에서 인터페이스 필드를 직렬화하는 문제 해결
- 예: `StageSO`의 `ItemGainEntry` 내부 클래스로 `ItemGain` struct를 Inspector에서 편집 가능하게 래핑

**[사례 2-12] 아이템 타입 계층의 다형성 설계**
```
IItemData (기본 아이템)
├─ IConsumableItem (소비 아이템, Consume() 메서드)
│  └─ IGainCurrencyItem (재화 획득 아이템, CurrencyType + Amount)
├─ IEquipmentItem (장비 아이템, CraftMaterials)
└─ IEquipmentMaterialItem (장비 재료 아이템, Marker Interface)
```
- 추상 클래스 `ItemDataSO` → `ConsumableItemSO`(abstract) → `GainCurrencyItemSO`(sealed)
- `EquipmentItemSO`, `EquipmentMaterialItem`은 `ItemDataSO`를 직접 상속
- `IEquipmentItem`이 `IItemData`도 구현하여 장비를 인벤토리 아이템으로도 취급 가능 (다중 인터페이스)

---

### 3. Unity3D 엔진에 대한 전반적인 이해

> 면접관 기대: "실행 순서, Scene 로딩, 초기화 순서를 설계해봤는가?"

#### 프로젝트 사례

**[사례 3-1] Scene 전환 시스템 (Additive Loading + Unload)**
- 파일: `Assets/Scripts/Infrastructure/SceneLoadingTask.cs`
- `LoadSceneAsync(sceneName, LoadSceneMode.Additive)` → `SetActiveScene` → `UnloadSceneAsync(oldScene)` 순서
- Additive 로딩으로 로딩 오버레이를 유지하면서 씬 전환 구현
- 3개의 씬(TitleScreen, Home, LoadingOverlay)이 유기적으로 전환

**[사례 3-2] 게임 프레임워크 초기화 설계**
- 파일: `Assets/Scripts/Dependency/Scopes/CoreLifetimeScope.cs`
- VContainer의 LifetimeScope를 활용한 계층적 초기화:
  1. `CoreLifetimeScope.Awake()` → Firebase 초기화, 세션 초기화, 에러 핸들러 등록
  2. 각 화면별 LifetimeScope가 부모-자식 관계로 연결
  3. 씬 로드 시 해당 LifetimeScope가 자동으로 DI 컨테이너 구성

**[사례 3-3] RuntimeInitializeOnLoadMethod를 활용한 싱글톤 초기화**
- 파일: `Assets/Scripts/Core/Supports/CoroutineHandler.cs:14-23`
```csharp
[RuntimeInitializeOnLoadMethod(RuntimeInitializeLoadType.BeforeSceneLoad)]
private static void InitializeSingleton()
{
    GameObject go = new GameObject(nameof(CoroutineHandler), typeof(CoroutineHandler));
    _runner = go.GetComponent<CoroutineHandler>();
    DontDestroyOnLoad(go);
}
```
- `BeforeSceneLoad` 타이밍으로 어떤 씬보다 먼저 초기화 보장
- `DontDestroyOnLoad`으로 씬 전환에도 파괴되지 않는 코루틴 실행기 유지

**[사례 3-4] 코루틴 기반 복합 로딩 시퀀스 관리**
- 파일: `Assets/Scripts/Gameplay/LoadingManager.cs`
- 로딩 페이드인 → 이벤트 대기(`WaitUntil`) → 씬 로드 대기 → 페이드아웃 순서를 코루틴으로 구현
- 이벤트 기반 동기화(`_TransitionReadiedEvent`, `_TransitionDisposedEvent`)로 UI 애니메이션과 로딩 상태 조율

**[사례 3-5] MonoBehaviour 생명주기를 고려한 UI Presenter 설계**
- 파일: `Assets/Scripts/UI/Core/UIPresenter.cs`
- `Awake()` → RegisterUICallbacks, `Start()` → SetupReferences/SetupSubscriptions/InitializeView, `OnDestroy()` → Dispose/DisposeSubscriptions, `OnEnable()` → InitializeView
- 각 생명주기에 적합한 초기화 작업을 분리

**[사례 3-6] PlayableDirector / Timeline 연동**
- 파일: `Assets/Scripts/UI/Screens/SummonAnimationScreen/SummonAnimationScreenView.cs`
- Timeline 기반 소환 애니메이션을 PlayableDirector로 제어
- `playableDirector.stopped +=` 이벤트로 애니메이션 종료 감지

---

### 4. UGUI 활용 능력

> 면접관 기대: "UGUI 성능 문제를 해결해봤는가?"

#### 프로젝트 사례

**[사례 4-1] MVP 패턴 기반 UI 시스템 전면 구축**
- 3개의 UI 코어 베이스 클래스 설계:
  - `UIView`: 순수 표시 담당 (Show/Hide, Dispose, RegisterUICallbacks)
  - `UIPresenter<TView>`: 자율적 UI 관리 (이벤트 구독, 뷰 초기화)
  - `UIComponent<TView>`: 외부 제어 가능한 UI (Show/Hide 외부 노출)
- View는 데이터를 알지 못하고, Presenter가 서비스 ↔ View 연결 담당

**[사례 4-2] 재사용 가능한 UI 컴포넌트 체계**
- `JustTextLabelView`: 6개 이상의 Presenter가 공유 (CoinLabel, GemLabel, FoodLabel, PlayerNameLabel, LevelLabel 등)
- `ButtonView`: 모든 버튼 View의 기반 클래스 (SummonButtonView, StageInfoButtonView 등이 상속)
- `IntegerSliderProgressView`: Slider + 텍스트를 조합한 범용 프로그레스바
- `NumericStepper`: 증감 버튼 + InputField를 결합한 범용 수치 입력 컴포넌트

**[사례 4-3] TextMeshPro 전면 적용**
- UGUI Text 대신 TextMeshProUGUI를 모든 UI 텍스트에 사용
- SDF 폰트 에셋 활용 (Maplestory Bold SDF, Maplestory Light SDF 등)
- 다양한 SDF 스타일 변형 제공 (No-Outline, Reward 등)

**[사례 4-4] 복잡한 UI 화면 구성 경험**
- `SoldierDetailScreen`: 탭 기반 페이지 전환 (InfoPage, LevelUpPage), 사도 스탯 리스트, 레벨업 비교 리스트, 장비 슬롯
- `ShopScreen`: 페이지 네비게이션 + 아이템 리스트 + 구매 시스템
- `BackpackScreen`: 인벤토리 아이템 슬롯 그리드
- `WorldMapScreen`: 스테이지 버튼 + 모달 팝업(좌/우 배치)
- `SummonScreen → SummonAnimation → SummonResult`: 3단계 연쇄 화면 전환

**[사례 4-5] HomeOverlay 스택 기반 화면 관리 시스템**
- 파일: `Assets/Scripts/UI/Screens/Home/HomeOverlaysController.cs`
- Stack 자료구조로 오버레이 화면을 관리 (Push/Pop)
- 뒤로가기 버튼으로 스택 Pop하여 이전 화면으로 복귀
- 홈 화면 위에 여러 오버레이(소환, 병사 목록, 상점, 월드맵 등)를 쌓는 구조

**[사례 4-6] Prefab 기반 UI 인스턴스화 시스템**
- `SoldierListView`, `ItemSlotListView`, `ShopItemButtonListView` 등에서 프리팹 기반 동적 리스트 생성
- 리스트 갱신 시 기존 인스턴스 Destroy 후 재생성 패턴 사용
- 90개 이상의 UI 프리팹 관리 (버튼, 아이콘, 리스트 아이템, 화면, 팝업 등)

**[사례 4-7] 팝업/모달 시스템 구현**
- `MenuPopup`: 메뉴 팝업 (배낭, 사기 충전 등 하위 메뉴)
- `MoraleRechargePopup`: 충전 횟수 조절 NumericStepper + 비용/결과 미리보기
- `RewardGainPopup`: 획득 아이템 목록 표시 (Timeline 연동 오프닝 애니메이션)
- `StageInfoModal`: 스테이지 정보 + 소탕/입장 기능 (좌/우 배치 변형)

**[사례 4-8] 프로필 프레임, 별 진행도 등 세밀한 커스텀 UI**
- `ProfileAvatarFrameComponent`: 아바타 + 프레임 조합 컴포넌트
- `StarProgress / StarProgressItem`: 별 아이콘 배열로 랭크 표시
- `SimpleIcon`: 아이콘 + 배경색 조합 범용 아이콘

---

### 5. Asset Pipeline 및 AssetBundle 구조 이해

> 면접관 기대: "Addressables / AssetBundle 실제 운용 경험?"

#### 프로젝트 사례

**[사례 5-1] ScriptableObject 기반 정적 데이터 파이프라인**
- `Assets/Settings/Game/` 디렉토리에 30개 이상의 .asset 파일로 게임 데이터 관리
- 병사, 스테이지, 챕터, 아이템, 상점, 소환 비용 등 모든 기획 데이터가 SO로 구성
- SO를 인터페이스로 추상화하여 런타임에서는 인터페이스로만 접근:
  - `SoldierSO` → `ISoldierData`, `SoldierDatabaseSO` → `ISoldierDatabase`
  - `ItemDatabaseSO` → `IItemDatabase`, `ChapterSO` → `IChapterData`

**[사례 5-2] Prefab 기반 씬 구성 아키텍처**
- `Assets/Prefabs/LifetimeScopes/`: 각 화면별 LifetimeScope 프리팹 (12개)
- `Assets/Prefabs/UI/Screens/`: 화면별 Canvas 프리팹 (11개)
- `Assets/Prefabs/UI/`: 버튼, 아이콘, 리스트 아이템 등 세분화된 프리팹 체계

> **부족한 점**: AssetBundle 또는 Addressables를 직접 사용한 코드는 프로젝트에 존재하지 않음. 다만 `com.unity.modules.assetbundle` 모듈이 manifest.json에 포함되어 있고, SO 기반 데이터 파이프라인은 AssetBundle 전환의 기반이 될 수 있음.

---

### 6. Scriptable Render Pipeline(SRP)에 대한 이해

> 면접관 기대: "URP 커스터마이징 경험? Built-in vs URP 차이?"

#### 프로젝트 사례

**[사례 6-1] URP 2D Renderer 환경 구축**
- `Packages/manifest.json:9`: `com.unity.render-pipelines.universal: 14.0.12` 사용
- `Assets/Settings/UniversalRP.asset` + `Assets/Settings/Renderer2D.asset`로 2D URP 파이프라인 구성
- `ProjectSettings/ProjectSettings.asset:50`: Linear Color Space 사용 (`m_ActiveColorSpace: 1`)

> **부족한 점**: 커스텀 Renderer Feature, 커스텀 셰이더, 포스트 프로세싱 커스터마이징 등의 SRP 심화 사용 사례는 프로젝트에 없음. HLSL 커스텀 셰이더도 없음 (`Assets/` 하위에 .hlsl 파일 0개, .shader 파일은 TMP 기본 셰이더만 존재).

---

### 7. Unity 성능 분석 도구(Profiler 등)에 대한 이해

> 면접관 기대: "실제 프로파일링으로 성능 개선한 Before/After 사례?"

#### 프로젝트 사례

**[사례 7-1] 성능을 고려한 struct 설계**
- `SoldierStatus`, `SoldierStatusf`, `ItemGain`, `SoldierEquipments` — 모두 struct로 설계
- 빈번한 스탯 계산/비교 시 GC 압력 최소화 의도

**[사례 7-2] 코루틴 WaitForSeconds 캐싱**
- `SummonAnimationScreenView`: `WaitForSeconds` 객체를 lazy 초기화하여 재사용

**[사례 7-3] 에러 보고 중복 방지를 위한 해시 기반 필터링**
- `UncaughtErrorCatcherService.VerifyDuplicatedError()`: FNV-1a 해싱으로 0.1초 내 중복 에러 필터링
- 에러 폭풍(error storm) 시 Crashlytics API 호출 빈도를 억제하는 성능 방어 코드

> **부족한 점**: 프로파일러를 사용한 구체적인 최적화 Before/After 사례가 코드에 직접 드러나지 않음. 다만 성능을 의식한 설계 흔적은 곳곳에 존재.

---

### 8. Build Pipeline에 대한 이해

> 면접관 기대: "CLI 빌드? 빌드 자동화? 버전 관리?"

#### 프로젝트 사례

**[사례 8-1] Firebase 빌드 설정 관리 경험**
- 커밋 히스토리: `add firebase android build settings`, `remove unnecessary gradle build setting`, `add edm4u and firebase plugin dependency`
- `Assets/google-services.json` + `Assets/StreamingAssets/google-services-desktop.json` 관리
- External Dependency Manager for Unity(EDM4U) 사용 (`Assets/ExternalDependencyManager/`)
- .gitignore에서 Firebase Android 플러그인을 관리 (빌드 환경별 분리)

> **부족한 점**: CLI 빌드 스크립트, Jenkins 파이프라인, 버전 자동화 등의 코드는 프로젝트에 포함되어 있지 않음.

---

### 9. Assembly Definition 및 컴파일 순서에 대한 이해

> 면접관 기대: "asmdef 구조 설계 경험? 컴파일 시간 감소 사례?"

#### 프로젝트 사례

**[사례 9-1] 13개 Assembly Definition으로 구성된 레이어드 아키텍처**

```
ProjectB.Core                    ← 핵심 타입 (InterfaceRef, ErrorData, CoroutineHandler)
ProjectB.Core.Editor             ← 에디터 전용 (InterfaceRefDrawer)
ProjectB.Data                    ← 인터페이스 + 타입 정의
ProjectB.Gameplay.Ports.Inbound  ← Inbound Port 인터페이스
ProjectB.Gameplay.Ports.Outbound ← Outbound Port 인터페이스
ProjectB.Gameplay.Ports.Internal ← Internal Port 인터페이스
ProjectB.Gameplay                ← 비즈니스 로직 (서비스 구현체)
ProjectB.UI                      ← UI Presenter / View / Component
ProjectB.UI.Services             ← UI 서비스 레이어
ProjectB.Infrastructure          ← 인프라 어댑터 (씬 로딩, Firebase, 세션 등)
ProjectB.Dependency.Installers   ← DI Installer (Port → MonoBehaviour 연결)
ProjectB.Dependency.Scopes       ← DI LifetimeScope 등록
ProjectB.Authoring.ScriptableObject ← SO 기반 데이터 저작
```

**[사례 9-2] asmdef 의존성 그래프 설계**
- `ProjectB.Gameplay`는 `ProjectB.Data`와 `ProjectB.Gameplay.Ports.*`에만 의존
- `ProjectB.UI`는 `ProjectB.Data`에 의존하지만 `ProjectB.Gameplay`에는 직접 의존하지 않음 (Port를 통한 간접 연결)
- `ProjectB.Infrastructure`는 Outbound Port에만 의존하여 Gameplay와 분리
- 순환 참조(circular reference) 없이 단방향 의존성 유지

**[사례 9-3] Editor/Runtime 코드 분리**
- `ProjectB.Core.Editor`는 별도 asmdef로 분리하여 빌드에 포함되지 않도록 설정
- `InterfaceRefDrawer` (CustomPropertyDrawer)가 런타임 코드와 분리

---

### 10. 소켓 프로그래밍에 대한 이해

> 면접관 기대: "TCP/UDP 통신 구현 경험? 패킷 직렬화?"

#### 프로젝트 사례

> **해당 사례 없음**: 프로젝트에 소켓 프로그래밍, 네트워크 통신 관련 코드가 존재하지 않음.
> 다만, 프로젝트 구조 자체가 서버 연동을 염두에 둔 설계임:
> - `ILoadPlayerDataPort`, `IInitializePlayerSessionPort` 등 데이터 로드/세션 관리가 Port로 추상화되어 있어, 로컬 저장소를 서버 API로 교체 가능한 구조
> - `PlayerSession` → `PlayerData` 계층으로 세션 기반 데이터 관리
> - TODO 주석들에 "플레이어 데이터 직렬화(JSON 저장 등)" 언급이 빈번하여 서버 통신 계획 존재

---

### 11. 문제 해결을 위한 능동적이고 주도적인 태도 / 신기술 학습 및 적용

#### 프로젝트 사례

**[사례 11-1] 아키텍처 반복적 개선 (커밋 히스토리 근거)**
```
e11c2a9 refactor: apply mvp architecture across ui system
f7fcb28 redefine of ui component into ui 'part'
659e3b2 refactor some ui presenter to ui component
9878093 refactor soldier status list to new ui structure
ec7695f refactor player soldier list and soldier list component to new ui structure
18a7b11 adjust namespace and reorganize directory
```
- MVP 아키텍처를 전면 적용한 후에도 UI Component 개념을 재정의하는 등 지속적 리팩토링

**[사례 11-2] VContainer(DI 프레임워크) 자발적 도입 및 적용**
- VContainer는 Unity에서 비교적 최신(2020~) DI 프레임워크
- Hexagonal Architecture와 조합하여 독자적인 StructuredLifetimeScope 체계 구축
- Template Method 패턴으로 DI 등록 순서를 강제하는 프레임워크 레벨 설계

**[사례 11-3] Firebase Crashlytics 기반 에러 보고 시스템 자체 구현**
- `GlobalErrorHandler` → `UncaughtErrorCatcherService` → `ReportErrorService` → Crashlytics
- Application.logMessageReceived + AppDomain.UnhandledException 이중 후킹
- 중복 필터링(FNV-1a), Fatal/Non-Fatal 분류, 커스텀 키 설정 등 실무 수준 구현

**[사례 11-4] Note 컴포넌트로 기술 부채 가시화**
- 파일: `Assets/Scripts/Core/Supports/Note.cs`
- `#if UNITY_EDITOR` 조건부 컴파일로 런타임 비용 없는 메모 시스템
- TODO 주석 외에 Inspector에서 시각적으로 확인 가능한 노트 시스템 구현

**[사례 11-5] InterfaceRef — Unity Inspector의 한계를 극복하는 커스텀 에디터 툴**
- Unity Inspector는 인터페이스 타입 필드를 직렬화할 수 없는 한계가 있음
- `InterfaceRef<T>` + `InterfaceRefDrawer`로 이 한계를 해결:
  - Inspector에서 Object를 드래그하면 인터페이스 구현 여부를 런타임 검증
  - GameObject를 드래그하면 해당 인터페이스를 구현한 Component를 자동 탐색
  - Reflection으로 제네릭 타입 인자를 추출하여 동적 검증

---

## 우대 사항별 사례 매칭

---

### 우대 1. Spine 사용 경험

#### 프로젝트 사례

**[사례 U1-1] Spine Runtime 패키지 통합**
- `Packages/manifest.json:3-4`:
```json
"com.esotericsoftware.spine.spine-csharp": "https://github.com/EsotericSoftware/spine-runtimes.git?path=spine-csharp/src#4.2",
"com.esotericsoftware.spine.spine-unity": "https://github.com/EsotericSoftware/spine-runtimes.git?path=spine-unity/Assets/Spine#4.2"
```
- Spine 4.2 버전의 C# Runtime + Unity Runtime 모두 Git URL로 직접 연동
- `Assets/Editor/SpineSettings.asset` 존재 → Spine 에디터 설정 구성 완료

**[사례 U1-2] Spine 기반 2D 캐릭터 에셋 활용**
- `Assets/ExternalAssets/Layer Lab/2D Minimal-BossMonster/Export/` 디렉토리에 JSON 형식의 Spine 데이터 파일 존재 (100.json ~ 104.json)
- Layer Lab의 2D Minimal Character/Boss Monster 에셋이 Spine 기반으로 동작

---

### 우대 2. 3rd Party 플러그인 관리 및 유지 보수 (담당 업무)

#### 프로젝트 사례

**[사례 U2-1] 다수의 외부 플러그인 통합 관리**
- **VContainer 1.17.0**: Git URL 기반 설치 (`jp.hadashikick.vcontainer`)
- **Spine 4.2**: Git URL 기반 설치 (spine-csharp + spine-unity)
- **Firebase**: SDK 직접 통합 + EDM4U(External Dependency Manager) 관리
- **URP 14.0.12**: Unity Package Manager
- **TextMeshPro 3.0.7**: Unity Package Manager
- **Timeline 1.7.7**: Unity Package Manager

**[사례 U2-2] Firebase 의존성 해결 경험**
- `Assets/gitignore_Android_의존성_해결.txt` 파일 존재 → 의존성 충돌 해결 문서화
- 커밋 히스토리: `remove firebase android plugin from repository`, `update gitignore to ignore firebase android plugins`
- EDM4U를 통한 Android 네이티브 의존성 자동 해결 설정

---

### 우대 3. 트릭컬 리바이브에 대한 높은 이해도 / 서브컬쳐 장르

#### 프로젝트 사례

**[사례 U3-1] 수집형 RPG 핵심 콘텐츠 시스템 구현**
트릭컬 리바이브와 동일한 장르의 핵심 시스템을 직접 구현한 경험:
- **소환(가챠) 시스템**: 1연차/10연차 소환, 젬 소비, 소환 애니메이션 + 결과 화면
- **사도(캐릭터) 수집 및 관리**: 사도 도감, 보유 사도 목록, 상세 정보 페이지
- **레벨업 시스템**: 식량(자원) 소비 → 경험치 변환 → 다단계 레벨업 로직
- **장비 시스템**: 장비 제작(재료 소비), 장비 장착(슬롯 기반)
- **스탯 계산 시스템**: 기본 스탯 × 성장률^레벨 공식, 전투력 가중합 계산
- **인벤토리 시스템**: 아이템 획득/소비/수량 관리, 배낭 UI
- **상점 시스템**: 페이지 기반 상점, 코인/젬 결제, 아이템 지급
- **소탕(Sweep) 시스템**: 사기(스태미나) 소비, 경험치/코인/아이템 보상 랜덤성 적용
- **사기(스태미나) 시스템**: 일일 충전 횟수 제한, 젬으로 충전, 최대치 검증
- **월드맵/챕터/스테이지**: 챕터 → 스테이지 계층 구조, 스테이지별 보상 설정

---

### 우대 4. 기타 (공고 개발 환경과의 일치도)

| 공고 개발 환경 | 프로젝트 현황 |
|---|---|
| Unity3D 2022.3 | Unity 2022.3 LTS 사용 (정확히 일치) |
| Rider | .idea 디렉토리 존재, DotSettings.user 파일 존재 → Rider 사용 확인 |
| Spine | Spine 4.2 런타임 통합 |
| C# | 프로젝트 주 언어 |
| Firebase | Firebase SDK + Crashlytics 통합 |
| Git | Git 저장소로 관리, 150개 이상 커밋 |

---

## 공고와 어울리지 않는 코드/구조 목록

> 이 공고는 **라이브 서비스 경험자, Unity 플랫폼 엔지니어**를 원합니다.
> 아래는 해당 관점에서 프로젝트에서 발견된 문제점입니다.

---

### 치명적 문제 (Critical)

#### [C-1] 데이터 영속화(Persistence) 레이어 완전 부재
- 프로젝트 곳곳에 `// TODO: 플레이어 데이터 직렬화(JSON 저장 등) 로직 필요` 주석이 반복됨
- 발견 위치:
  - `SoldierEquipService.cs:54`
  - `CraftEquipmentService.cs:50`
  - `InternalInventoryService.cs:44, 91, 131`
- **영향**: 앱 종료 시 모든 플레이어 데이터가 소실됨
- **공고 관점**: 라이브 서비스에서 데이터 저장/로드는 가장 기본적인 기능. 이 부분의 부재는 "상용화 경험"에 대한 의문을 야기할 수 있음

#### [C-2] 네트워크/서버 통신 레이어 완전 부재
- 소켓 프로그래밍, REST API 호출, 서버 인증 등 네트워크 관련 코드가 전무
- `ILoadPlayerDataPort` 구현체(`LoadPlayerDataService`)가 하드코딩된 더미 데이터 반환 추정
- **공고 관점**: "소켓 프로그래밍에 대한 이해"가 지원 자격인데, 프로젝트에 관련 코드 없음

#### [C-3] CoroutineHandler.Stop() 메서드의 버그
- 파일: `Assets/Scripts/Core/Supports/CoroutineHandler.cs:37-48`
```csharp
public static void Stop(Coroutine coroutine)
{
    foreach (var c in _runner._coroutines)
    {
        if (coroutine == c)
        {
            _runner.StopCoroutine(c);
        }
    }
    _runner._coroutines.Clear(); // <-- 모든 코루틴 참조를 삭제함!
}
```
- 하나의 코루틴을 중지하려고 했는데, `Clear()`가 `if` 블록 밖에 있어 모든 코루틴 참조가 삭제됨
- **공고 관점**: 코드 품질에 대한 의문. 실무에서 이런 버그는 추적하기 어려운 간헐적 문제를 유발

#### [C-4] MenuServicePortInstaller 타입 불일치 버그
- 파일: `Assets/Scripts/Dependency/Installers/MenuServicePortInstaller.cs`
- `public class MenuServicePortInstaller : DIBasedPortInstaller<MenuServicePortInstaller>`
- `DIBasedPortInstaller<IMenuServicePort>`가 아닌 자기 자신을 타입 파라미터로 넘김
- DI 주입 시 Port가 아닌 Installer 자체가 주입되는 버그

#### [C-5] InvasionSettingSO.GetLevelsOfStage() 루프 변수 버그
- 파일: `Assets/Scripts/Authoring/ScriptableObject/Invasion/InvasionSettingSO.cs:39`
```csharp
for (int j = 0; i < c.Stages.Count; j++)  // i가 아니라 j여야 함!
```
- 내부 루프의 조건이 `j < c.Stages.Count`가 아닌 `i < c.Stages.Count`로 되어 있음
- 외부 루프 변수 `i`를 참조하여 **무한 루프** 발생 가능
- **공고 관점**: 스테이지 레벨 조회 기능이 완전히 동작하지 않는 치명적 버그

---

### 구조적 문제 (Structural)

#### [S-1] AssetBundle / Addressables 사용 경험 부재
- 프로젝트에 AssetBundle이나 Addressables 코드가 전혀 없음
- 지원 자격에 "Asset Pipeline 및 AssetBundle 구조 이해"가 명시되어 있음
- 모든 에셋이 직접 참조(Direct Reference)로 연결됨 → 대규모 프로젝트에서는 메모리/로딩 문제 발생

#### [S-2] SRP/셰이더 커스터마이징 경험 부재
- URP를 사용하지만 기본 설정만 적용
- 커스텀 셰이더, Renderer Feature, 포스트 프로세싱 커스터마이징 없음
- 지원 자격에 "Scriptable Render Pipeline(SRP)에 대한 이해"가 있는데, 프로젝트에서 단순 사용 이상의 경험이 보이지 않음

#### [S-3] 빌드 자동화 / CI/CD 부재
- Jenkins 파이프라인, 빌드 스크립트, 버전 자동화 코드 없음
- 공고의 개발 환경에 Jenkins가 명시되어 있음

#### [S-4] 테스트 코드 완전 부재
- 단위 테스트, 통합 테스트, 플레이 모드 테스트가 프로젝트에 전혀 없음
- `com.unity.test-framework`는 설치되어 있으나 사용하지 않음
- 인터페이스 기반 설계로 Mock 테스트가 가능한 구조임에도 테스트 미작성

#### [S-5] WorldMapScreenLifetimeScope가 아키텍처 패턴을 따르지 않음
- 다른 모든 화면이 `StructuredLifetimeScope`를 상속하는데, WorldMap만 `LifetimeScope`를 직접 상속
- Port 등록이 없는 빈 클래스
- 아키텍처 일관성 위반

#### [S-6] Infrastructure 서비스에서 FindObjectsOfType 사용 (DI 우회)
- `LoadBackpackScreenService`, `LoadSoldierDetailScreenService`, `LoadSummonScreenService`, `LoadSummonAnimationScreenService`, `LoadSummonResultScreenService` 등 다수
- DI 컨테이너가 있음에도 `FindObjectsOfType<HomeOverlaysControlService>()`로 씬 내 서비스 검색
- 주석에 TODO로 DI 전환 필요성을 인지하고 있으나 미해결
- **공고 관점**: Hexagonal Architecture를 잘 구축해놓고 일부 Infrastructure 서비스에서 Service Locator 안티패턴으로 우회하는 모순

#### [S-7] Database SO에서 반복되는 캐싱 로직 중복
- `AvatarDatabaseSO`, `SoldierDatabaseSO`, `ItemDatabaseSO`가 모두 동일한 `Dictionary<string, T>` 캐싱 패턴 사용
- 제네릭 `DatabaseSO<T>` 베이스 클래스로 추상화 가능하나 미적용
- 코드 중복이 DRY 원칙 위반

---

### 코드 품질 문제 (Code Quality)

#### [Q-1] InventoryService.Items의 매 접근마다 ToArray() 호출
- 파일: `Assets/Scripts/Gameplay/InventoryService.cs`
```csharp
public IReadOnlyList<IReadOnlyPlayerItem> Items =>
    _playerSessionHolderPort.GetPlayerSession().PlayerData.Items.ToArray();
```
- 프로퍼티 접근마다 새 배열 할당 → UI가 매 프레임 호출 시 GC 압력 급증
- TODO 주석도 있음: "메모리 큰일남. 인벤토리 더티체크 필요"
- **공고 관점**: "Unity 성능 분석 도구에 대한 이해"를 요구하는 공고에서 이런 코드는 마이너스

#### [Q-2] PlayerDataService.GetTotalCombatPower() 캐싱 없음
- 매 호출마다 모든 병사의 전투력을 Sum() — O(n) 연산 반복
- UI에서 빈번히 호출될 수 있는 메서드인데 캐싱 전략 없음

#### [Q-3] LINQ 사용이 성능 민감 경로에 존재
- `SoldierLevelUpService.ConsumeFoods()`: `FirstOrDefault()` 3번 호출 (GetConsumeFoodAmount, GetNextLevelStatus, GetNextLevelCombatPower에서도 각각)
- `InternalInventoryService`: 모든 메서드에서 `FirstOrDefault()`로 아이템 검색
- 소규모에서는 문제없지만 아이템/병사 수가 늘면 성능 저하 가능
- **공고 관점**: "C# 런타임 특성(GC)" 이해를 요구하는 공고에서 LINQ의 boxing/allocation 문제는 면접 질문 소재가 될 수 있음

#### [Q-4] SweepService에서 Dictionary 이중 조회
- `Assets/Scripts/Gameplay/SweepService.cs:93-96`
```csharp
if (itemGains.ContainsKey(itemGain.item))
    itemGains[itemGain.item] += itemGain.quantity;
else
    itemGains[itemGain.item] = itemGain.quantity;
```
- `ContainsKey` + 인덱서 접근 = Dictionary를 2번 조회. `TryGetValue`로 1번에 처리 가능

#### [Q-5] 리스트 UI에서 Object Pooling 미사용
- `SoldierListView`, `ItemSlotListView`, `ShopItemButtonListView` 등에서 갱신 시 `Destroy()` → `Instantiate()` 반복
- 수집형 RPG에서 캐릭터/아이템 목록은 매우 빈번하게 갱신되는 UI
- Object Pooling 미적용은 GC Spike의 직접적 원인

#### [Q-6] Static Event 사용으로 테스트 불가능한 UI 이벤트
- `MenuButtonEvents`, `SoldierDetailButtonEvents`, `BackpackNavigateButtonEvents` 등에서 `static Action` 이벤트 사용
- 정적 이벤트는 Mock 교체가 불가능하고, 구독 해제 누락 시 메모리 릭 발생
- **공고 관점**: 정적 이벤트의 메모리 릭 위험성은 "C# 런타임 이해" 면접 질문과 직결

#### [Q-7] SoldierLevelUpService의 하드코딩 상수
- `Assets/Scripts/Gameplay/SoldierLevelUpService.cs:18`
```csharp
private const float FOODS_CONSUME_RATIO = 0.3f;
// TODO: SO 기반 Setting으로 분리하기
```
- 기획 데이터가 코드에 하드코딩 → 밸런스 조정 시 코드 수정 + 재빌드 필요
- 다른 설정 데이터들은 모두 SO로 관리하고 있으면서 이것만 상수로 남음

#### [Q-8] SummonManager의 다중 책임 위반
- `ISummonServicePort`와 `ISummonAnimationManagerPort` 두 개의 Port를 동시에 구현
- 소환 로직 + 애니메이션 조율이 한 클래스에 혼재
- SRP(단일 책임 원칙) 위반

#### [Q-9] 에러 처리의 Silent Failure 패턴
- 대부분의 서비스에서 에러 발생 시 `Debug.LogError()` + `return`으로 조용히 실패
- 호출자(UI)는 연산 실패 여부를 알 수 없음 → 사용자에게 피드백 불가
- 예: `SoldierEquipService.Equip()` 실패 시 UI가 이를 감지할 방법 없음

#### [Q-10] ILoadHomePort의 추상화 누수(Leaky Abstraction)
- 코드 주석에 직접 "계층 간 추상화 및 의존성 역전이 이루어지지 않은 안티 패턴"이라고 명시되어 있음
- `ILoadingTaskPort`를 반환하여 Gameplay 레이어가 Infrastructure의 로딩 상세를 알게 됨

#### [Q-11] SoldierDetailScreenPresenter의 God Class 경향
- 170줄 이상, 5개 이상의 서비스 의존, 데이터 변환 로직 직접 수행
- 코드 내 TODO 주석으로 리팩토링 필요성을 인지하고 있음

#### [Q-12] FirebaseInitializer의 ContinueWith 사용
- `Assets/Scripts/Infrastructure/FirebaseInitializer.cs:16`
- `ContinueWith`는 메인 스레드가 아닌 백그라운드 스레드에서 콜백 실행
- Unity API(`Debug.Log` 등)는 메인 스레드에서만 안전 → `ContinueWithOnMainThread` 사용 권장
- 초기화 실패 시 대응 로직 미구현 (`// TODO: Firebase 초기화 실패 대응`)

#### [Q-13] LoadingManager의 이벤트 구독 메모리 릭 가능성
- `_TransitionReadiedEvent += () => isTransitionReadied = true` (코루틴 내부)
- 코루틴이 반복 호출될 경우 람다 구독이 누적될 수 있음
- 이벤트 해제 코드 없음

#### [Q-14] Crasher 클래스의 존재
- 파일: `Assets/Scripts/DevUtils/Crasher.cs`
- 무한 재귀(`Recursive(0)`)와 의도적 Exception throw
- 개발/테스트용이지만 프로덕션 빌드에 포함될 경우 보안/안정성 위험
- `#if UNITY_EDITOR` 또는 `#if DEBUG` 가드가 없음

#### [Q-15] GainCurrencyItemSO.Consume()의 미구현 placeholder
- `GainCurrencyItemSO.Consume()` 메서드가 `Debug.LogError("not implemented")`만 출력
- 소비성 아이템의 핵심 기능이 미구현 상태

#### [Q-16] PlayerSoldier의 Level 타입이 short
- `IReadOnlyPlayerSoldier.Level`이 `short` 타입
- 코드 내 TODO: "그냥 int로 바꾸기. 메모리 2byte 줄여봐야 의미 없음"
- 실제로 `SoldierStatusComputer.GetNextLevelStatus()`에서 `(short)(playerSoldier.Level + 1)` 캐스팅이 반복되어 코드 가독성 저하
- 과도한 최적화(premature optimization)의 사례

#### [Q-17] PlayerData의 Int32.MaxValue 오버플로우 체크
- `PlayerData.AddIntValueInternal()`에서 `Int32.MaxValue` 체크 후 `Debug.LogError`만 출력하고 값을 변경하지 않음
- 오버플로우 발생 시 사용자에게 알림 없이 데이터 증가가 무시되는 silent failure

#### [Q-18] SoldierSO가 ISoldierData와 ISoldierCardDisplaySetting을 동시에 구현
- 한 ScriptableObject가 데이터 정의와 UI 표시 설정을 동시에 담당
- 데이터 계층과 프레젠테이션 계층이 같은 객체에 혼재
- ISP(인터페이스 분리 원칙) 관점에서는 분리했으나, 구현 객체가 동일하여 실질적 분리 효과 제한적

---

### 부재/미완성 항목 요약

| 공고 요구사항 | 프로젝트 현황 | 심각도 |
|---|---|---|
| AssetBundle 구조 이해 | AssetBundle/Addressables 코드 없음 | 높음 |
| 소켓 프로그래밍 이해 | 네트워크 코드 없음 | 높음 |
| SRP 이해 | URP 기본 설정만 사용 | 중간 |
| Profiler 이해 | 프로파일링 사례 코드 없음 | 중간 |
| Build Pipeline 이해 | 빌드 스크립트/자동화 없음 | 중간 |
| iOS/Android 네이티브 플러그인 | 네이티브 플러그인 코드 없음 | 낮음(우대) |
| 글로벌 서비스 경험 | 다국어/타임존 처리 없음 | 낮음(우대) |

---

## 최종 요약: 강점과 보완점

### 이 프로젝트가 보여주는 강점
1. **아키텍처 설계 역량**: Hexagonal Architecture + DI + MVP 조합은 모바일 게임 프로젝트에서 매우 드문 수준의 구조적 성숙도
2. **OOP/SOLID 실전 적용**: 추상화 없이는 불가능한 구조를 직접 설계하고 구현
3. **수집형 RPG 도메인 이해**: 소환, 레벨업, 장비, 상점, 소탕 등 트릭컬 리바이브와 겹치는 콘텐츠를 직접 구현
4. **Assembly Definition 활용**: 13개 asmdef의 레이어드 의존성 그래프는 대규모 프로젝트 경험의 증거
5. **에디터 툴 제작**: InterfaceRef + CustomPropertyDrawer로 Unity Inspector 한계 극복
6. **Firebase 통합 및 에러 보고**: Crashlytics 기반 에러 보고 파이프라인 자체 구축
7. **Spine 통합**: 패키지 수준 연동 경험

### 면접 전 반드시 보완해야 할 점
1. **데이터 영속화 레이어** 최소한 JSON 직렬화 + 로컬 저장 구현
2. **AssetBundle/Addressables** 관련 기능 최소 1개 시연 가능 사례
3. **소켓/네트워크** 관련 별도 프로젝트 또는 코드 샘플 준비
4. **CoroutineHandler.Stop() 버그** 수정
5. **MenuServicePortInstaller 타입 버그** 수정
6. **InvasionSettingSO.GetLevelsOfStage() 무한루프 버그** 수정
7. **성능 최적화 Before/After** 사례 준비 (Profiler 스크린샷 + 수치)
8. **FindObjectsOfType 사용 제거** — Infrastructure 서비스를 DI로 전환

---

### 수치 요약

| 항목 | 수량 |
|---|---|
| 지원 자격별 매칭 사례 | 총 48개 |
| 우대 사항별 매칭 사례 | 총 14개 |
| 공고와 어울리지 않는 항목 | 총 30개 (치명적 5 + 구조적 7 + 코드품질 18) |
| 부재/미완성 항목 | 7개 |
